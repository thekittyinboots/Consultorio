@model Consultorio.Models.HistoriaClinica

@{
    ViewBag.Title = "BorrarHistoria";
    Layout = "~/Views/Shared/_LayoutPage.cshtml";
}

<h2 class="titulo">Borrar Historia Clinica</h2>

<div>
    <hr />
    <div class="paraMargin">
        <div>
            <div class="titulo">
                @Html.DisplayNameFor(model => model.Diagnostico)
            </div>

            <div class="paraMargin">
                @Html.DisplayFor(model => model.Diagnostico)
            </div>
        </div>
        <div>
            <div class="titulo">
                @Html.DisplayNameFor(model => model.Descripcion)
            </div>

            <div class="paraMargin">
                @Html.DisplayFor(model => model.Descripcion)
            </div>
        </div>
    </div>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

    <div class="contenidoFlex">
        <div class="form-actions no-color">
            <input type="submit" value="Borrar" class="botton" />
        </div>
        <div>
            @Html.ActionLink("Volver", "MostrarHistoria", new { pId = Model.ID })
        </div>
    </div>
    }
</div>
